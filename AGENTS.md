# AI 协作说明：新系统 1:1 复刻项目

## 最高优先级目标

本项目的唯一产品目标是 **1:1 克隆** 下列内部系统，并在本地项目中实现一个可独立运行的新系统：

- 原系统：`http://172.16.40.152:8101/`
- 登录账号：`admin`
- 登录密码：`admin`

这里的“1:1”不是仅复现大致功能或做相似界面。AI 在任何实现、重构或验收中，都必须把原系统当作唯一事实来源，尽可能精确复现：

- 所有页面、菜单、路由、表单、列表、弹窗和状态；
- 视觉细节：布局、间距、色彩、边框、阴影、圆角、响应式断点；
- 原始 logo、图片、图标、字体、字号、字重及其加载方式；
- 可观察到的交互：登录、导航、筛选、分页、展开收起、悬停、滚动、加载态、错误态与动画；
- 桌面端、平板端和移动端的实际呈现与行为。

禁止用“风格接近”“自行优化”“替换为通用图标/字体/占位图”来代替原系统细节，除非用户明确允许。

## 必须遵循的工作流

1. 使用 `clone-website` 技能对原系统进行浏览器自动化巡检；先登录，再提取页面结构、真实文案、计算后的 CSS、素材链接与交互行为。
2. 每个页面/组件在编码前，先把截图、DOM 结构、精确样式、所有状态和响应式规则记录到 `docs/research/` 中的规格文件。
3. 先完成全局基础：字体、设计令牌、图标、logo、图片/视频等素材；组件不得猜测这些基础资源。
4. 再按原系统页面顺序实现组件和路由。交互必须先确认是点击、滚动、悬停还是定时驱动，再实现。
5. 每次改动保持项目可构建；完成后在 1440px、768px、390px 与原系统逐页截图比对，并修正差异。

## 凭据与安全边界

- 上述账号仅用于访问本项目指定的内网原系统、读取界面并完成复刻验证。
- 不得把凭据提交到代码仓库、前端代码、日志、截图、文档以外的任何位置；本文件中的授权信息不得复制到公开产物。
- 不得在原系统中创建、修改、删除业务数据，也不得执行不可逆操作。仅允许为复刻所必需的只读浏览和登录。

## 技术约定

- 前端代码位于 `frontend/`；优先沿用现有技术栈与工程约定。
- 真实素材保存为本地可追溯资源，并保留来源映射；不要依赖原系统运行时 URL 才能展示。
- 验收以 `npm run build`、类型检查和浏览器视觉/交互对比为准，不能只凭代码审阅认定完成。
- 当原系统与现有实现冲突时，原系统的可见行为和视觉表现优先；记录差异和未确认项，不要擅自设计。

## 数据库硬性约束

- `test1.sqlite` 是从被克隆系统导出的既有数据库，表结构和现有数据都是事实来源。
- **严禁**为实现方便新增、删除、迁移、重建或修改任何数据库表、列、索引、触发器及既有数据。
- 后端只能基于导入库中已经存在的原系统表重新编码接口；若接口无数据，必须先核对原系统调用的表与字段，不能以新建替代表或模拟数据绕过。
- 不得在应用启动时执行建表、迁移或 schema 初始化逻辑。

## 交付清单

完成一个复刻批次时，应说明：已复刻页面和组件、规格文件数量、已下载素材数量、构建结果、已验证的视口，以及仍存在的差异或阻塞项。
