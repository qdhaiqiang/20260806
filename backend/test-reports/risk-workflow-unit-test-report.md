# 核心风险处理单元测试报告

| 项目 | 结果 |
| --- | --- |
| 执行时间 | 2026-08-09 18:46:52 +0800 |
| 执行命令 | `cd backend && clojure -M:test` |
| 测试命名空间 | `risk-api.workflow-test` |
| Test case | 10 |
| Assertions | 84 |
| Failures | 0 |
| Errors | 0 |
| 结论 | 通过 |

## 覆盖范围

1. 已入库企查查动态解析、原始记录追溯、缺失动态和重复解析幂等。
2. 二级企业风险的情况描述提交、到期自动提交、重复提交及全部不允许状态的拒绝校验。
3. 风险确认提交仅允许 `UNCONFIRMED`，并验证其余 7 个状态均被拒绝。
4. 风险确认审核矩阵：`是风险/不是风险` 分别在`通过/不通过`下正确流转至 `CONFIRMED` 或 `CLOSED`，且重复审核被拒绝。
5. 处置计划仅允许 `CONFIRMED`，并验证目标、截止时间和步骤必填校验。
6. 进度、完成提交、处置审核的不存在步骤、错误状态、通过和驳回分支。
7. 消除申请仅允许 `CONFIRMED`；消除审核验证通过至 `ELIMINATED`、驳回恢复 `CONFIRMED` 与错误状态拒绝。
8. 风险等级调整验证非已确认状态、相同等级、缺失申请、审核通过、审核不通过和重复审核。
9. 操作日志、消息记录落库，以及所有入口的非法状态校验。

## 未覆盖 / 尚未实现的文档要求

- 处置逾期扫描、季度提醒、定时推送与消息重试任务。
- 基于真实登录用户的按钮权限、管理主体数据范围、指标主管部门权限。
- 企业纵向链路、横向关联部门、风险接收人和重大风险领导的真实通知寻址。
- 前端尚未具备处置审核、消除审核、等级调整等页面入口，因此这些节点目前由后端单元测试验证。

## 原始运行摘要

```text
Ran 10 tests containing 84 assertions.
0 failures, 0 errors.
```
